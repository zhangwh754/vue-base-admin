export default {
  // 避免在箭头函数参数周围使用括号，即使参数是多行的。
  arrowParens: 'avoid',
  // 在对象和数组字面量中的括号内使用空格。
  bracketSpacing: true,
  // JSX标签的闭合括号放置在新行上，而不是同一行末尾。
  jsxBracketSameLine: false,
  // 指定每行代码的最大字符数，超过这个数值代码会尝试换行。
  printWidth: 100,
  // 保持段落文本的原始格式，不进行换行。
  proseWrap: 'preserve',
  // 不使用分号来结束语句。
  semi: false,
  // 使用单引号代替双引号。
  singleQuote: true,
  // 设置制表符宽度为2个空格。
  tabWidth: 2,
  // 不使用制表符，而是使用空格来缩进代码。
  useTabs: false,
  // 尾随逗号的使用规则，'es5'表示在ES5环境下使用尾随逗号。
  trailingComma: 'all',
}
